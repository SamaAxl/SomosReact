import React from "react";
import ReactDOM from "react-dom";
import Profile from "./App.js";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(<Profile />);

//una documentada no estaria mal de vez en cuando
